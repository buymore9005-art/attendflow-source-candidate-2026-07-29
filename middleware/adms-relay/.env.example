# Public Supabase Edge Function base. Do not append /iclock/cdata here.
UPSTREAM_BASE_URL=https://PROJECT_REF.supabase.co/functions/v1/adms
LISTEN_HOST=0.0.0.0
LISTEN_PORT=8080
DEVICE_TOKENS_FILE=device-tokens.json
REQUEST_TIMEOUT_MS=20000
MAX_BODY_BYTES=2100000
# Only for a local fake upstream during development; never use in production.
ALLOW_INSECURE_UPSTREAM=false
